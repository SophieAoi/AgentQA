id: TC_DOOH_006
title: Edit Planner campaign and re-sync to Adserver
description: >
  Edit an existing campaign in Planner and re-sync it to Adserver, then
  verify the updated details reflect correctly in Influence.
preconditions:
  - requires login
  - "GAP: requires access to a separate external system called \"Planner\" — this project has no configured URL, credentials, or any other access to it; not currently available. Source sheet also marks this case Automated: No."
suite: Planner
